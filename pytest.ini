[pytest]
pythonpath = .
testpaths = tests
# The suite is hermetic by default. test_live_mirrors.py is the one exception and
# talks to real mirrors, so it's deselected unless asked for by name: pytest -m live
addopts = -m "not live"
markers =
    live: hits real mirrors over the network (deselected by default)
